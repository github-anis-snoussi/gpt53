# DNS Server Configuration
PORT=5333
HOST=127.0.0.1
DNS_TTL=30
ENABLE_LOGGING=true
DEFAULT_TXT_RESPONSE="This wasn't supposed to happen… but here we are."

# Authentication Configuration
# Set your 10-character API key for authenticating DNS queries
API_KEY=myapikey12

# OpenAI Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
