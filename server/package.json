{
  "name": "gpt53-server",
  "version": "1.0.0",
  "description": "Basic TypeScript DNS server using dns2 (TXT Records Only)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "watch": "ts-node --watch src/index.ts",
    "clean": "rm -rf dist"
  },
  "keywords": ["dns", "server", "typescript", "txt-records"],
  "author": "Anis Snoussi <anis@snoussi.me>",
  "license": "ISC",
  "dependencies": {
    "dns2": "^2.1.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 